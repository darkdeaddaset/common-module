server:
  port: 8080

spring:
  liquibase:
    change-log: classpath*:db/changelog.xml
    enabled: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/log?currentSchema=logging
    username: postgres
    password: 1234